# ==============================================================================
# OpenClaw Railway Template - Environment Variables
# ==============================================================================
# Railway automatically detects variables from .env.example
# Variables with ${{...}} syntax are auto-generated by Railway on deploy
# For local development, copy this to .env and fill in values manually
# ==============================================================================

# Required: Password to access the /setup wizard and Control UI
SETUP_PASSWORD=${{secret(20)}}

# Required: Auth token for the gateway (treat as admin secret)
OPENCLAW_GATEWAY_TOKEN=${{secret(32)}}

# Server port (must match Railway HTTP Proxy port)
PORT=8080
OPENCLAW_PUBLIC_PORT=8080

# Persistent volume paths (Railway volume must be mounted at /data)
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# Runtime config (do not change unless you know what you're doing)
OPENCLAW_NODE=bun
INTERNAL_GATEWAY_PORT=18789
INTERNAL_GATEWAY_HOST=127.0.0.1

# Optional: Pin OpenClaw version (default: main)
# OPENCLAW_GIT_REF=main
